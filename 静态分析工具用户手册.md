# 静态分析工具用户手册

## 1. 工具简介

静态分析工具（页面名称为“GJB8114 在线静态分析”）是 UniPortal 一体化测试平台中的静态分析子工具，面向 C/C++ 软件工程，依据 GJB8114 编码规则检查源代码中的潜在问题。

用户可以从 UniPortal 项目库选择工程，也可以直接上传 C/C++ 源码或 ZIP 工程压缩包。分析完成后，页面展示缺陷总数、问题级别、命中规则、文件统计和缺陷明细。对于可写的 UniPortal 项目，结果将写入业务项目目录下的 `ct8114` 文件夹，供后续工具使用。

| 阶段     | 功能                                                         |
| -------- | ------------------------------------------------------------ |
| 项目选择 | 从 UniPortal 项目库选择工程，或直接上传源码/ZIP              |
| 静态分析 | 调用 DCAB / DeepSITRServer 执行 GJB8114 规则扫描             |
| 结果展示 | 展示问题总数、ERROR/WARNING、规则统计、文件统计和缺陷列表    |
| 结果写回 | 生成 `last_report.json` 和 `meta.json`，写入项目的 `ct8114` 目录 |

## 2. 核心技术

### 2.1 整体架构

```text
浏览器前端页面
        ↓ HTTP API
静态分析工具后端服务
        ↓
DCAB / DeepSITRServer 静态分析服务
        ↓
GJB8114 规则扫描与结果解析
        ↓
UniPortal 共享卷 / 项目目录 / ct8114 结果文件
```

工具采用前后端分离的浏览器应用架构。前端负责项目选择、任务提交、状态展示和结果查看；后端负责读取工程源码、创建分析任务、调用分析服务、解析结果并完成报告写回。

### 2.2 前端功能

前端页面主要提供以下功能：

- 加载并展示当前 UniPortal 工程下的项目；
- 查看项目中已识别的 C/C++ 文件；
- 直接上传源码文件或 ZIP 工程压缩包；
- 提交静态分析任务并显示执行状态；
- 展示分析汇总、规则统计、文件统计和缺陷明细；
- 按关键字、问题级别和规则筛选缺陷；
- 查看项目最近一次历史报告；
- 将当前分析结果导出为 JSON 文件。

### 2.3 后端分析服务

后端服务负责：

- 从 UniPortal 共享卷读取项目源码；
- 校验并整理用户上传的源码或 ZIP 工程；
- 创建和管理分析任务；
- 调用 DCAB / DeepSITRServer 静态分析服务；
- 解析规则编号、问题级别、文件位置和问题描述等信息；
- 生成结构化分析报告；
- 将结果写回 `ct8114/last_report.json` 和 `ct8114/meta.json`。

### 2.4 GJB8114 规则体系

工具主要依据 GJB8114 编码规则检查 C/C++ 源代码。当前配置下，全量规则数量为 204 条；实际启用数量和命中数量以分析结果页面显示为准。

分析结果包含规则编号、检查器名称、问题级别、文件路径、行号、列号和问题描述等结构化信息，便于用户筛选、统计和定位问题。

### 2.5 UniPortal 集成与结果写回

访问地址中的 `portal_project_id` 用于确定当前 UniPortal 工程。工具会根据该参数读取对应工程共享卷中的项目列表，并只展示当前工程下的可分析项目。

对于可写项目，分析结果将保存到业务项目目录下的：

```text
ct8114/last_report.json
ct8114/meta.json
```

`last_report.json` 保存结构化分析报告，`meta.json` 保存项目名称、分析时间和结果摘要等信息。页面显示“已写回共享卷”时，表示结果已经保存成功。已完成分析的项目可通过“查看历史报告”重新加载最近一次分析结果。

## 3. 使用前准备

### 3.1 访问方式

方式 A：从 UniPortal 工具中心进入静态分析工具。

方式 B：使用浏览器独立访问：

```
http://<host>:8003/static/index.html?portal_project_id=<工程UUID>
```

`portal_project_id` 用于确定当前 UniPortal 工程，建议保留该参数，不要手工删除。缺少该参数时，UniPortal 项目库可能为空，直接上传也将作为临时分析处理。

### 3.2 支持的代码格式

| 类型            | 支持格式                              |
| --------------- | ------------------------------------- |
| C 源码/头文件   | `.c`、`.h`                            |
| C++ 源码/头文件 | `.cc`、`.cpp`、`.cxx`、`.hpp`、`.hxx` |
| 工程压缩包      | `.zip`                                |

当前版本不支持 Python 源码分析。

### 3.3 项目要求

- 项目中应至少包含一个 `.c`、`.cc`、`.cpp` 或 `.cxx` 源文件；
- 只有头文件或没有可识别的 C/C++ 源文件时，可能无法正常启动分析；
- ZIP 压缩包解压后仍须包含可识别的 C/C++ 源文件；
- 上传 ZIP 时应只选择一个 ZIP 文件。

## 4. 核心操作步骤

整体流程：

```text
进入工具 → 选择或上传项目 → 启动静态分析 → 查看分析结果
```

### 步骤 1：进入工具

从 UniPortal 工具中心或带有 `portal_project_id` 的标准地址进入工具。

页面打开后，会自动加载当前 UniPortal 工程下的项目列表。首页包括“项目库”“直接上传”和分析结果展示区域。项目库中会显示当前工程下的项目名称、项目状态、文件数量、历史分析状态和问题数量等信息。

【放图：项目库首页，展示工具标题、项目库、项目卡片、分析入口和右侧分析汇总区域】

### 步骤 2：选择项目或上传工程

#### 从项目库选择

在“项目库”中选择待分析项目。项目卡片会显示项目名称、项目 ID、文件数量、历史分析状态和问题数量等信息。

点击“查看文件”，可以确认系统已识别的 C/C++ 文件。用户还可以填写可选的入口文件；留空时，系统将分析项目中的全部可识别源文件。

项目已有分析记录时，可以点击“查看历史报告”加载最近一次结果。

【放图：选中项目并展开文件列表，展示已识别的源码文件、入口文件输入框和分析按钮】

#### 直接上传

“直接上传”是可选兼容方式，可上传多个 C/C++ 源码文件或单个 ZIP 工程压缩包。正式项目建议优先使用“项目库”。

标准地址包含 `portal_project_id` 时，可以选择在分析后将上传工程加入项目库；否则本次上传将作为临时分析，不写回 UniPortal 共享卷。

【可选放图：直接上传页面，展示源码/ZIP 上传区域、入口文件输入框和开始分析按钮】

### 步骤 3：启动静态分析

选择项目后，点击“开始分析”或“开始分析项目”。

系统将创建分析任务并调用后端静态分析服务。任务执行期间，页面会显示“正在提交分析任务”或“轮询中”等状态。任务完成后，页面会自动展示分析结果。

分析过程中请勿连续重复提交同一项目。

【放图：分析任务执行状态，展示项目卡片处于分析中状态以及页面轮询提示】

### 步骤 4：查看分析结果

任务完成后，右侧结果区域将展示：

- 问题总数；
- WARNING 和 ERROR 数量；
- 检查项种类；
- 文件数量；
- GJB8114 全量规则数量；
- 实际命中规则数量；
- 按级别、按规则和按文件统计；
- 缺陷明细及共享卷写回状态。

页面显示“已写回共享卷”时，表示分析结果已成功保存到项目的 `ct8114` 目录。

【放图：分析结果汇总，展示问题总数、WARNING/ERROR、规则数量、文件数量、报告路径和“已写回共享卷”状态】

用户可以继续查看规则命中、文件统计和缺陷明细。文件统计展示各源文件的行数、语句数、函数数、注释行数和问题数量。缺陷列表展示问题级别、规则编号、文件、行列位置、问题描述和检查器名称。

【放图：文件统计和缺陷明细，展示 files_stats 表格以及缺陷列表】

用户还可以使用搜索框、级别筛选和规则筛选缩小结果范围，通过“查看历史报告”加载已有结果，或点击“导出 JSON”保存当前报告。

【放图：缺陷筛选与结果导出，展示搜索框、级别筛选、规则筛选和导出 JSON 按钮】

## 5. 分析结果说明

### 5.1 分析汇总

- 问题总数：本次分析发现的问题总量；
- WARNING：警告级问题数量；
- ERROR：错误级问题数量；
- 检查项种类：本次实际命中的规则种类；
- 文件数量：报告中包含的文件数量；
- GJB8114 全量规则：本次配置加载的规则数量；
- 实际命中规则：产生问题记录的规则数量。

### 5.2 规则与文件统计

规则统计支持按问题级别和规则查看命中数量。规则较多时，可以展开全部规则。

文件统计展示各文件的路径、代码行数、语句数、函数数、注释行数和问题数量。

### 5.3 缺陷列表

缺陷列表展示问题级别、规则编号、文件、行列位置、问题描述和检查器名称。

用户可以：

- 搜索规则、检查器、文件或问题描述；
- 只查看 ERROR 或 WARNING；
- 只查看指定规则对应的问题。

文件和行列信息可用于辅助定位，但当前页面不提供源码正文查看或点击缺陷跳转源码的功能。

### 5.4 历史报告与导出

项目存在历史结果时，可以点击“查看历史报告”，无需重新分析。

点击“导出 JSON”可以将当前结构化报告保存到本地。当前版本不提供 Word 或 PDF 报告导出。

## 6. 注意事项

1. 当前版本主要面向 C/C++ 工程，不支持 Python 源码分析。
2. 项目必须包含可识别的 C/C++ 源文件，只有头文件时不能正常启动分析。
3. 分析时间与项目规模和规则数量有关，大型项目可能需要等待数分钟。
4. 分析任务执行期间，请勿连续重复提交同一项目。
5. 建议从带有 `portal_project_id` 的标准地址进入工具。
6. 页面显示“已写回共享卷”时，才表示结果已经成功保存。
7. “直接上传”是可选兼容入口，正式项目优先使用“项目库”。
8. JSON 报告可能包含项目路径和缺陷信息，对外传递前应进行脱敏处理。
